name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  python-unit:
    name: Python (pytest, unit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v9
      - run: uv run --extra test pytest tests/ -m unit
