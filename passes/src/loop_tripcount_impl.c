/**
 * Stub implementation of __loop_tripcount marker.
 * This function does nothing at runtime - it exists only to provide
 * a definition for linking and to carry trip count information in IR.
 */
void __loop_tripcount(int max_iterations) {
    (void)max_iterations;
}
